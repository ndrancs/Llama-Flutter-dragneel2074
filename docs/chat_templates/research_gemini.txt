Definitive Technical Report on LLM Chat Template Standardization for GGUF DeploymentThis report provides a prescriptive reference for implementing production-ready chat templates across nineteen large language model families, focusing on optimal formatting, special token definitions, advanced reasoning protocols, and agentic function-calling support. The instructions are optimized for GGUF model deployment within a mobile (Flutter/Android) environment leveraging llama.cpp.I. Foundational Framework for GGUF Prompting1.1. The Role of Jinja2 in llama.cpp Production PipelinesThe robust deployment of modern large language models (LLMs) requires structured input prompts that extend beyond simple text concatenation. Due to the fine-tuning process, models expect dialogue turns to be separated and delineated by specific control tokens or delimiters. This structure is often managed using the Jinja2 templating language, which is supported natively by Hugging Face transformers and incorporated into llama.cpp for GGUF models.1The Necessity of Conditional LogicSimple string concatenation fails when attempting to support optional elements, such as system prompts or tool definitions. Jinja2 provides the essential conditional logic ({% if system_prompt is defined %}) required to render the prompt dynamically based on the input messages list.2 For instance, if a system prompt is absent, the template must ensure that the role delimiters (e.g., <|start_header_id|>system<|end_header_id|>) are also omitted, preventing the model from receiving empty or malformed role blocks.GGUF Metadata and Template PriorityFor production pipelines, reliance on external template files is often impractical. Instead, the preferred method is to embed the standardized chat template string directly into the GGUF file’s metadata (tokenizer.chat_template). The llama.cpp pipeline can then prioritize this internal configuration, or developers can explicitly override it using the --chat-template-file flag when running the server or inference application.4 This dependency on template code embedded in the GGUF file means developers must verify the template’s integrity during the initial conversion process.1.2. Token Standardization and Role Mapping (The Atomic Delimiter Trend)The evolution of prompt formatting demonstrates a clear shift away from complex, whitespace-sensitive string delimiters towards atomic, token-ID-backed control tokens. This transition significantly enhances template robustness.Defining BOS, EOS, and EOTEvery LLM requires a Beginning-of-Sequence (BOS) token (e.g., <s> or <|begin_of_text|>) and an End-of-Sequence (EOS) token (e.g., </s> or <eos>).5 However, modern models introduce a crucial distinction: the End-of-Turn (EOT) token.Llama 3.x: Uses <|eot_id|> to explicitly separate full dialogue turns. This is generated by the model after its response and before the subsequent user input.5Gemma 2: Uses <end_of_turn> in conjunction with <eos> (as <end_of_turn><eos>) to explicitly signal the conclusion of a message, providing two layers of segmentation.7The use of explicit EOT tokens minimizes ambiguity in multi-turn conversations, preventing the model from misinterpreting subsequent user input as a continuation of its own previous statement.Role Tokenization and the Header SystemLegacy models relied on composite delimiters, such as Llama 2’s <<SYS>>\n...<</SYS>>, which are highly prone to formatting errors. In contrast, newer models employ atomic delimiters:Llama 3.x: Uses <|start_header_id|> and <|end_header_id|> tokens to encapsulate roles (system, user, assistant).5 This structure is: <|start_header_id|>role<|end_header_id|>\n{content}<|eot_id|>.ChatML Variants (Qwen, Hermes): Rely on the single <|im_start|> and <|im_end|> tokens to designate role boundaries.6This token-centric approach means the challenge for mobile deployment shifts from meticulous string manipulation to meticulous verification of the GGUF file itself. If the GGUF conversion fails to correctly map these unique token strings (found in the original model’s tokenizer.json or added_tokens.json) to their corresponding special token IDs, the prompt structure will collapse, leading to incoherent output and model failure in production.The following table summarizes the critical tokens necessary for correct formatting across key model families:Table I: Critical Special Token Mapping for GGUF ModelsModel FamilyBOS TokenEOS/EOT TokenSystem Delimiters (Raw)User Delimiters (Raw)Assistant Delimiters (Raw)Llama 2<s></s><<SYS>>...<</SYS>>...N/A (Starts after ``)Llama 3.x`<begin_of_text>``<eot_idMistral/Mixtral<s></s>Implicit/None...N/A (Starts after ``)DeepSeek V3.1 (Non-Think)<｜begin·of·sentence｜><｜end·of·sentence｜>{system prompt}<｜User｜><｜Assistant｜>Gemma 2Implicit/None<end_of_turn><eos>None (Role-based)<start_of_turn>user<end_of_turn><start_of_turn>modelQwen 2.5 (ChatML)N/A (Optional <s>)`<im_end>``<1.3. Implementing add_generation_prompt Logic in llama.cppFor any chat model, the final prompt sent to the LLM must be precisely formatted to indicate that the next token the model generates should belong to the assistant role. This is often achieved in toolkit libraries via an add_generation_prompt=True flag.8In a production deployment, this means the prompt sequence for the current turn must terminate exactly at the beginning of the expected model response structure, without a trailing EOT token. For Llama 3.x, the required ending is: <|start_header_id|>assistant<|end_header_id|>\n. For Qwen, the ending is <|im_start|>assistant. If this termination is omitted or incorrectly followed by an EOT token, the model may attempt to generate further user or system turns, resulting in failure or security concerns (prompt injection). This step is non-negotiable for initiating a coherent response.II. Core Family Templates: Meta and Mistral Ecosystems2.1. Llama 2 (The Legacy White-space Standard)The Llama 2 instruction-tuned models follow a specific, highly rigid chat template that requires precise handling of special tokens, newlines, and white-space.9The Encapsulation StructureThe Llama 2 format uses a nested bracketing structure. The entire system prompt is enclosed within <<SYS>> and <</SYS>>, which is, in turn, encapsulated within the first user instruction block, bounded by and.Full Turn Structure (Single Turn with System):<s> <<SYS>>\n{system_prompt}\n<</SYS>>\n\n{user_prompt}Multi-Turn Structure (Subsequent Turns):{assistant_response}</s><s> {user_prompt}Production Warning: Whitespace RigidityLlama 2 is uniquely sensitive to minute formatting errors, particularly concerning newlines (\n) and spaces. Documentation explicitly recommends invoking strip() on all input prompts to eliminate accidental double-spaces, which can degrade performance and coherence.10 The Llama 2 structure is widely recognized as a template approach that is highly prone to implementation errors compared to the more robust, token-delimited formats that succeeded it.2.2. Llama 3.1 and Llama 3.3 (The Modern Tokenized Standard)Llama 3.x models (including Llama 3.1 and the enhanced Llama 3.3 70B variant) leverage the atomic token header system to create a significantly more reliable chat format.5 Llama 3.3 70B, which is text-only, supersedes the instruction-tuned Llama 3.1 70B model and uses the same core prompt format.11Adopting Header IDsThe template begins with the <|begin_of_text|> token. Every message is then enclosed by the appropriate header IDs and terminated by the End-of-Turn token, <|eot_id|>. The roles supported include system, user, assistant, and ipython.5Llama 3 Multi-Turn Dialogue Structure:<|begin_of_text|><|start_header_id|>system<|end_header_id|>
{system_prompt}<|eot_id|>
<|start_header_id|>user<|end_header_id|>
{user_message_1}<|eot_id|>
<|start_header_id|>assistant<|end_header_id|>
{assistant_message_1}<|eot_id|>
<|start_header_id|>user<|end_header_id|>
{user_message_2}<|eot_id|>
<|start_header_id|>assistant<|end_header_id|>
The final line is the generation prompt, ensuring the model immediately begins responding under the assistant role. The shift from Llama 2’s composite delimiters to Llama 3’s tokenized headers dramatically reduces template fragility, enabling simpler and more reliable prompt construction within the Flutter plugin logic.2.3. Mistral/Mixtral Instruction Templates (V0.1, V0.2, V0.3)The Mistral family of models maintains a simpler, bracket-based instruction format derived from its original instruction fine-tuning.V0.1/V0.2 Chat FormatThe standard instruction format surrounds user input with and tokens, delimited by the BOS (<s>) and EOS (</s>) tokens.12Mistral Multi-Turn Structure:<s> {user_message_1}{assistant_message_1}</s> {user_message_2}Since the Mistral V0.1 and V0.2 instruction models were not explicitly trained on a dedicated system token, system prompts must be carefully integrated, typically by prepending them to the entire dialogue or embedding them within the first `` block. For deployments prioritizing high fidelity, using models trained with dedicated system tokens (like Llama 3 or Qwen) is generally preferred.Mistral V0.3 TransitionMistral-7B-Instruct-v0.3 incorporates an extended vocabulary (32768 tokens) and uses a V3 tokenizer. Most importantly for agentic development, it natively supports function calling.13 This newer version requires developers to adapt their tooling to handle the updated tokenizer and the specific protocols necessary for tool use (discussed in Section V).III. Reasoning and Agentic Model Templates (Complexity Management)Some advanced LLMs, particularly those designed for complex planning and problem-solving, utilize internal reasoning chains which must be programmatically managed by the client application.3.1. DeepSeek R1/V3.1 (Hybrid Thinking Mode and the Streaming Bug)DeepSeek V3.1 is specifically designed as a hybrid model, capable of supporting both "thinking mode" (reasoning) and "non-thinking mode," controlled entirely by the applied chat template.14Non-Thinking Mode PrecisionThe non-thinking template relies on distinct bracketed tokens but incorporates a structural artifact: the closing reasoning tag, </think>.DeepSeek V3.1 Non-Thinking Prefix:<｜begin·of·sentence｜>{system prompt}<｜User｜>{query}<｜Assistant｜></think>.14The inclusion of </think> here, despite reasoning being suppressed, indicates that the model's architecture is fundamentally designed to generate structured reasoning. Therefore, omitting this token in non-thinking mode may cause the model to generate unstructured, non-compliant output.Thinking Mode Protocol and Streaming ChallengesWhen generating reasoning chains, the model output is bounded by the <think> and </think> tags.3 However, a critical production challenge exists when using llama.cpp for streaming inference: the output often misses the starting <think> tag while retaining the closing </think> tag.4This asymmetry breaks programmatic output parsing, preventing the client application from reliably extracting the final, non-reasoning answer. To ensure production readiness on a mobile device, the Flutter application must implement client-side injection logic. This mitigation requires the client stream parser to detect the start of verbose, multi-stage generation (which characterizes reasoning output) and automatically prefix the content stream with the missing <think> token before passing the text to the display or final output parser. The client must assume template output flaws when streaming advanced models.3.2. Qwen Families (QwQ-32B Reasoning and History Pruning)The Qwen families, including QwQ-32B, utilize the common ChatML format but introduce distinct, crucial requirements for managing reasoning outputs. The QwQ-32B model generates full reasoning chains bounded by <think>...</think> tags.16Confirmed Missing Start TokenLike DeepSeek, QwQ-32B presents a documented challenge: the Qwen team confirmed that the model does not naturally output the first <think> token.16 Consequently, production implementations must enforce programmatic injection of a “<think>\n” prefix at the beginning of the model’s generation, ensuring the reasoning content is correctly wrapped and parsable.16Mandatory History PruningThe most significant architectural requirement for QwQ-32B is the mandate to exclude all reasoning content from the input context of subsequent turns. Including the reasoning chains or <think>...</think> blocks in the chat history submitted to the model significantly degrades output quality.16This necessitates that the mobile application deploy a dual history structure:A full display history, retaining the reasoning blocks for user visibility.A pruned model history, where the client rigorously identifies and strips out all content between and including the <think> and </think> tags before tokenization and prompt re-submission.Table II: Reasoning Model Protocol and Edge Case MitigationModel FamilyReasoning Start TokenReasoning End TokenHistory Pruning RequirementStreaming Edge Case (GGUF)Client-Side MitigationDeepSeek R1/V3.1<think></think>Exclude internal reasoning blocks.Missing <think> start token possible in llama.cpp streaming.Implement stream lookahead/prefixing logic to inject <think> at the start of generated output.Qwen QwQ-32B<think></think>Mandatory exclusion of <think>...</think> blocks from subsequent context.Model does not generate the first <think> token naturally.Client must enforce programmatic injection of <think>\n prefix when initiating generation.IV. Emerging and Specialized Structures4.1. Gemma 2 and Gemma 1.1 (Explicit Turn Segmentation)The Gemma models, particularly Gemma 2, implement a unique, highly segmented turn structure designed for maximum clarity in conversational dialogue. Gemma models use the tokens <start_of_turn> and <end_of_turn> to define the boundaries of each message.17Gemma 2 Multi-Turn Dialogue Structure:<start_of_turn>user
{user_message_1}<end_of_turn>
<start_of_turn>model
{assistant_message_1}<end_of_turn><eos>
<start_of_turn>user
{user_message_2}<end_of_turn>
<start_of_turn>model
Termination RigorGemma 2 improves upon its predecessor by requiring the model to explicitly end its generation with two distinct tokens: <end_of_turn><eos>.7 This dual termination mechanism ensures robust stability in multi-turn contexts. By defining both the conversational end (<end_of_turn>) and the sequence end (<eos>), Gemma 2 minimizes the likelihood of the model generating ambiguous continuations or truncated outputs, which is a key benefit for memory-constrained edge deployments.4.2. ChatML and Other Bracketed VariantsThe industry utilizes several other standardized bracketed formats, often dictated by specific training objectives. It is vital to recognize that templates often vary dramatically within a single vendor’s ecosystem.DeepSeek Coder V2/V3.1 InconsistencyIn contrast to DeepSeek’s tokenized reasoning models, the DeepSeek Coder V2 and V3.1 instruction models use a distinct, colon-delimited format:DeepSeek Coder V2/V3.1 Structure:<｜begin of sentence｜>{system_message} User: {user_message_1} Assistant: {assistant_message_1}<｜end of sentence｜>User: {user_message_2} Assistant:.19This contradiction—where the Coder variant uses non-tokenized strings while the reasoning variant uses highly specialized tokens—confirms that developers cannot generalize templates across model purpose (Chat vs. Code vs. Reasoning), even within the same vendor. The Flutter plugin must dynamically look up the exact template required based on the GGUF model's specific name.Other ChatML DerivativesQwen 2.5: A pure ChatML variant using <|im_start|> and <|im_end|> delimiters.6Phi 3.5 Mini (Microsoft): Uses simple angled brackets for role definition: <|system|>, <|user|>, <|assistant|>.6InternLM2: Adheres to the ChatML pattern but is often preceded by a BOS token (<s>).6V. Advanced Agentic Template Protocols and ImplementationModern model templates must incorporate structured tool (function) definitions to enable agentic workflows. This requires careful integration of tool schemas and strict adherence to output format constraints.5.1. Standardized Tool/Function Calling via Jinja2Implementing tool use relies on Jinja2’s ability to conditionally inject the available function definitions into the system prompt when the tools parameter is present.3The standard workflow involves three critical steps:Tool Definition Injection: The client provides the list of available tools (in JSON schema format) within the system message (or through a specialized section of the prompt).11Model Tool Call: The model generates a structured response (e.g., JSON or prescriptive string) indicating which function(s) to call and with what parameters.21Tool Result Injection: The client executes the function and injects the result back into the conversation history using a dedicated tool role, allowing the model to generate the final, context-aware answer.225.2. Llama 3.3 Zero-Shot Function Calling ProtocolLlama 3.3 supports zero-shot function calling by placing the JSON function schema directly within the Llama 3-standard system message.11Tool Definition Placement:The system message must be dynamically constructed to include a preamble instructing the model on its role, followed by the JSON list of available functions.11Model Output Syntax Constraint:The Llama 3.3 protocol mandates that if the model decides to invoke a function, the output must adhere to a prescriptive, non-JSON bracketed format: [func_name1(params_name1=params_value1, params_name2=params_value2...)].11 The model is explicitly instructed not to return any other text in the response if a function call is made. This constraint minimizes parsing ambiguity on the client side, requiring only simple string pattern matching rather than a full JSON parser.The subsequent tool result is injected using the Llama 3 tool role:<|start_header_id|>tool<|end_header_id|>\n{tool_content}<|eot_id|>.225.3. Mistral V0.3 Function Calling RequirementsMistral-7B-Instruct-v0.3 also supports function calling and follows a protocol generally compliant with the Hugging Face and OpenAI formats, typically outputting a JSON object detailing the function call.13Tool ID ComplianceA critical implementation detail for Mistral v0.3 is the mandatory requirement for tool call identifiers. The internal training constraints of the model require that tool call IDs generated by the model, and subsequently used by the client when feeding back results, must be exactly 9 alphanumeric characters.13 Failure to adhere to this strict length and character set when injecting the tool response back into the history will lead to predictable failure in the agentic loop. The Flutter application must incorporate robust input validation and strict adherence to this length constraint during tool execution feedback.5.4. Hermes 2.5 and Gorilla OpenFunctions ProtocolsHermes 2.5 XML WrappingThe Hermes 2.5 Pro model uses a ChatML foundation for chat but mandates a specific XML structure for function definitions. The system prompt must contain the function signatures wrapped within <tools></tools> XML tags.23 The model output for a tool call is then structured as a separate <tool_call> JSON object </tool_call> block.23 This use of XML provides a clear hierarchical boundary for tool data within the prompt.Gorilla OpenFunctions V2Gorilla models, particularly OpenFunctions V2, are natively trained for complex agentic tasks, including parallel and multiple function calls supporting various API languages (Python, Java, REST).24 Gorilla emphasizes structured output, often returning a fully formatted JSON object containing all required function calls.21 The focus here is on achieving a highly reliable, parsable API response, simplifying client-side extraction but requiring specialized model training.Table III: Advanced Agentic and Tool-Calling ProtocolsModel FamilyTool Definition PlacementTool Call Format (Model Output)Tool Result Role/DelimiterRequired Tool ID SpecificityLlama 3.3System Message (JSON Schema)Prescriptive: [func_name(params...)]`<start_header_idMistral v0.3System (JSON Schema, Template Injection)OpenAI/HF format (JSON)Role: tool (via standard templates)Tool Call ID must be exactly 9 alphanumeric characters.13Hermes 2.5System Message (Wrapped in <tools></tools> XML)<tool_call> JSON object </tool_call>N/A (Model response structure)N/A (Focus on XML wrapping)Gorilla V2External function listStructured JSON (Supports parallel calls)N/A (Focus on JSON output)N/A (Focus on standardized API object)VI. Conclusions and Recommendations for Production Deployment6.1. Architectural Mandates for Mobile PromptersThe analysis of 19 LLM families and their required chat templates confirms that high-fidelity mobile deployment requires dynamic template switching and specialized client-side correction logic, particularly for reasoning models.The Shift to Atomic TokensThe industry trend away from the complex, rigid composite delimiters (Llama 2) toward atomic, token-ID-based headers (Llama 3, Gemma 2) dramatically improves prompt stability. For GGUF deployment, this means the primary integration challenge shifts from manipulating strings and managing hidden whitespace to ensuring the core tokenizer configuration within the GGUF file correctly maps all necessary special tokens. Developers must prioritize models using these tokenized headers for long-term production viability.Criticality of Client-Side Correction for ReasoningFor models like DeepSeek R1 and Qwen QwQ-32B, reliance on the model or llama.cpp to correctly generate the initial reasoning boundary token (<think>) is unreliable under streaming conditions.4 A production-ready Flutter plugin must implement compensatory stream parsing logic that dynamically detects the start of unstructured reasoning output and programmatically injects the required starting token. Without this measure, structured parsing of internal thought processes cannot be guaranteed.Mandatory Dual History ManagementDeploying models like QwQ-32B imposes a unique architectural requirement: the mobile application must maintain two distinct versions of the conversation history. One version is displayed to the user (including thinking blocks), while the other—the context submitted back to the model for the next turn—must be rigorously pruned of all reasoning content (<think>...</think>). Failure to perform this history sanitation step will result in measurable degradation of the model’s performance in multi-turn conversations.166.2. GGUF Deployment and Mobile Optimization NotesWhen configuring llama.cpp for Android deployment, developers are advised to use the embedded GGUF template metadata whenever possible. If external Jinja templates are required, the --jinja and -fa (force attention) flags should be used to ensure template processing and optimize performance for supported models.4 Given the memory and processing constraints of mobile devices, careful conditional logic in the Jinja templates—proactively omitting function definitions or system prompts when not strictly necessary—is essential for minimizing context length and maximizing inference speed. Finally, strict validation of micro-requirements, such as Mistral V0.3’s 9-character alphanumeric tool call IDs, must be embedded within the agentic workflow management components of the Flutter application.